#!/usr/bin/perl

$JAVA = "/usr/bin/java";
$RHINO_JAR = "~/Library/Java/Extensions/js.jar";
$RHINO_CLASSPATH = "org.mozilla.javascript.tools.shell.Main";
$OPTIONS = "";
foreach (@ARGV) {
	if (/^-/) { $OPTIONS .= "$_ "; }
	else { $OPTIONS .= "'$_' "; }
}

system("$JAVA $RHINO_CLASSPATH $OPTIONS");
